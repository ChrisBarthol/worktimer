<% provide(:title, 'Add a Customer') %>
<h1>Add a Customer</h1>

<div class="row">
  <div class="span6 offset3">
    <%= form_for(@customer) do |f| %>
      <%= render 'customerfields', f: f %>
      <%= f.submit "Add Customer", class: "btn btn-large btn-primary" %>
    <% end %>
  </div>
</div>